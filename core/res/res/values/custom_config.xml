<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: 2026 Penguin Project
     SPDX-License-Identifier: Apache-2.0
-->
<resources xmlns:xliff="urn:oasis:names:tc:xliff:document:1.2">
    <!-- The list of components which should be automatically disabled for a specific device.
     Note: this MUST not be used to randomly disable components, ask for approval first! -->
    <string-array name="config_deviceDisabledComponents" translatable="false">
    </string-array>

    <!-- The list of components which should be automatically disabled for all devices. -->
    <string-array name="config_globallyDisabledComponents" translatable="false">
    </string-array>

    <!-- The list of components which should be forced to be enabled. -->
    <string-array name="config_forceEnabledComponents" translatable="false">
    </string-array>

   <!-- Defines the actions shown in advanced reboot submenu -->
   <string-array name="config_restartActionsList">
       <item>restart</item>
       <item>restart_systemui</item>
       <item>restart_recovery</item>
       <item>restart_bootloader</item>
   </string-array>

   <!-- Whether to allow process with media UID to access CameraServiceProxy -->
   <bool name="config_allowMediaUidForCameraServiceProxy">false</bool>
   
   <!-- Component name of the activity that allows choosing the usb function when a usb device is
         plugged in. -->
    <string name="config_usbFunctionActivity" translatable="false"
            >com.android.systemui/com.android.systemui.usb.UsbFunctionActivity</string>

    <!-- Whether device has an alert slider. -->
    <bool name="config_hasAlertSlider">false</bool>

    <!-- PowerOffAlarmService -->
    <bool name="config_powerOffAlarmEnabled">false</bool>

    <!-- Whether to force enable multi resolution configuration for camera -->
    <bool name="config_forceMultiResolution">false</bool>

    <!-- Array of:
        - Path to fast charging status file
        - Delimiter ";"
        - Value if fast charging is active
        Used to detect whether an oem fast charger is active
        Example: <item>/sys/class/power_supply/battery/fastchg_status;1</item>
    -->
    <string-array name="config_oemFastChargerStatusPaths" translatable="false">
    </string-array>

    <!-- CPU temperature -->
    <string name="config_cpu_temp_path">/sys/class/thermal/thermal_zone0/temp</string>
    <integer name="config_sysCPUTempMultiplier">1</integer>
    <integer name="config_sysBatteryTempMultiplier">1</integer>
</resources>
